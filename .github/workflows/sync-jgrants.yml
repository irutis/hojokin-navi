name: J-Grants データ同期

on:
  schedule:
    - cron: '0 0 * * *'  # 毎日 JST 09:00
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - name: 環境変数の確認
        run: |
          echo "SUPABASE_URL設定: ${{ secrets.SUPABASE_URL != '' }}"
          echo "SUPABASE_SERVICE_KEY設定: ${{ secrets.SUPABASE_SERVICE_KEY != '' }}"
      - run: node scripts/sync-jgrants.mjs
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
